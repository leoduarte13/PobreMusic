import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig={appId:'app.pobremusic.player',appName:'PobreMusic',webDir:'dist',server:{androidScheme:'https'}};
export default config;
