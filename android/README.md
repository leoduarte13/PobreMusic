# PobreMusic Android

Native Android player architecture for Spotify playlist metadata import and background playback.

Build with Gradle/Android Studio. The app uses Media3 ExoPlayer + MediaSessionService for Android background playback.
