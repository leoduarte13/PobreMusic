import React, { useCallback, useEffect, useRef, useState } from "react";
import { Navbar } from "./components/Navbar";
import { PlaylistInput } from "./components/PlaylistInput";
import { TrackList } from "./components/TrackList";
import { AudioPlayerBar } from "./components/AudioPlayerBar";
import { YouTubeIFrameContainer, YouTubePlayerRef } from "./components/YouTubeIFrameContainer";
import { SpotifyAuthModal } from "./components/SpotifyAuthModal";
import { ConfigGuideModal } from "./components/ConfigGuideModal";
import { Track, PlaylistData, ConfigStatus, PlaybackStatus, SpotifyUser, UserPlaylistSummary } from "./types";
import { fetchPlaylistSafe, getCandidateBackendUrls, resolveYouTubeVideoIdClient } from "./utils/clientMusicResolver";
import { playlistLogger } from "./utils/logger";
import { AlertCircle, Disc3, Lock, LogIn } from "lucide-react";

function normalizePlaylist(p: any): UserPlaylistSummary {
  return { id: String(p?.id || ""), name: String(p?.name || "Playlist sem nome"), description: p?.description || "", isPrivate: p?.isPrivate ?? p?.public === false ?? false, isCollaborative: Boolean(p?.isCollaborative ?? p?.collaborative), trackCount: Number(p?.trackCount ?? p?.total_tracks ?? p?.items?.total ?? p?.tracks?.total ?? 0), cover: p?.cover || p?.image_url || p?.images?.[0]?.url || "", ownerName: p?.ownerName || p?.owner?.display_name || "Você" };
}

export default function App() {
  const [configStatus, setConfigStatus] = useState<ConfigStatus | null>(null); const [configOpen, setConfigOpen] = useState(false); const [spotifyAuthOpen, setSpotifyAuthOpen] = useState(false); const [spotifyUser, setSpotifyUser] = useState<SpotifyUser | null>(null); const [userPlaylists, setUserPlaylists] = useState<UserPlaylistSummary[]>([]); const [isLoadingUserPlaylists, setIsLoadingUserPlaylists] = useState(false); const [playlistData, setPlaylistData] = useState<PlaylistData | null>(null); const [tracks, setTracks] = useState<Track[]>([]); const [isLoadingPlaylist, setIsLoadingPlaylist] = useState(false); const [playlistError, setPlaylistError] = useState<string | null>(null); const [needsAuthNotice, setNeedsAuthNotice] = useState(false); const [currentTrackIndex, setCurrentTrackIndex] = useState<number | null>(null); const [playbackStatus, setPlaybackStatus] = useState<PlaybackStatus>("unstarted"); const [currentTime, setCurrentTime] = useState(0); const [duration, setDuration] = useState(0); const [volume, setVolume] = useState(80); const [isMuted, setIsMuted] = useState(false); const [previousVolume, setPreviousVolume] = useState(80); const [shuffle, setShuffle] = useState(false); const [repeatMode, setRepeatMode] = useState<"off" | "all" | "one">("all");
  const playerRef = useRef<YouTubePlayerRef>(null); const tracksRef = useRef<Track[]>([]); const indexRef = useRef<number | null>(null); const shuffleRef = useRef(false); const repeatRef = useRef<"off" | "all" | "one">("all");
  useEffect(() => { tracksRef.current = tracks; }, [tracks]); useEffect(() => { indexRef.current = currentTrackIndex; }, [currentTrackIndex]); useEffect(() => { shuffleRef.current = shuffle; }, [shuffle]); useEffect(() => { repeatRef.current = repeatMode; }, [repeatMode]);
  const getToken = () => { try { return localStorage.getItem("spotifyTokenManual") || localStorage.getItem("spotifyTokenManuaL") || ""; } catch { return ""; } };
  const fetchUserPlaylists = useCallback(async () => { setIsLoadingUserPlaylists(true); try { const token = getToken(); const headers: Record<string,string> = {}; if (token) headers.Authorization = `Bearer ${token}`; const response = await fetch("/api/my-playlists", { headers, credentials:"include", cache:"no-store" }); const data = await response.json().catch(() => ({})); if (!response.ok) { if (response.status === 401) setSpotifyUser(null); setUserPlaylists([]); return; } setUserPlaylists(Array.isArray(data.playlists) ? data.playlists.map(normalizePlaylist) : []); } catch { setUserPlaylists([]); } finally { setIsLoadingUserPlaylists(false); } }, []);
  const checkSpotifyAuth = useCallback(async () => { try { const token=getToken(); const headers:Record<string,string>={}; if(token) headers.Authorization=`Bearer ${token}`; const response=await fetch("/api/auth/me",{headers,credentials:"include",cache:"no-store"}); const data=await response.json().catch(()=>({})); if(data.authenticated&&data.user){setSpotifyUser(data.user);fetchUserPlaylists();}else{setSpotifyUser(null);setUserPlaylists([]);} } catch {} },[fetchUserPlaylists]);
  useEffect(() => { fetch("/api/config-status",{cache:"no-store"}).then(r=>r.json()).then(setConfigStatus).catch(()=>{}); checkSpotifyAuth(); },[checkSpotifyAuth]);
  const handleSpotifyLoginSuccess=(user:SpotifyUser)=>{setSpotifyUser(user);setSpotifyAuthOpen(false);setNeedsAuthNotice(false);fetchUserPlaylists();};
  const logoutSpotify=async()=>{try{localStorage.removeItem("spotifyTokenManual");localStorage.removeItem("spotifyTokenManuaL");}catch{} try{await fetch("/api/auth/logout",{method:"POST",credentials:"include"});}catch{} setSpotifyUser(null);setUserPlaylists([]);};
  const loadPlaylist=useCallback(async(input:string)=>{const value=input.trim();if(!value)return;setIsLoadingPlaylist(true);setPlaylistError(null);setNeedsAuthNotice(false);playlistLogger.startLoad(value,getCandidateBackendUrls());try{const {data,needsAuth}=await fetchPlaylistSafe(value,getToken()||null);if(needsAuth)setNeedsAuthNotice(true);if(!data?.sucesso||!Array.isArray(data.faixas)||data.faixas.length===0)throw new Error(data?.descricao||data?.error||"Não foi possível carregar as faixas. Conecte o Spotify para playlists privadas.");setPlaylistData(data);setTracks(data.faixas);setCurrentTrackIndex(0);setCurrentTime(0);setDuration(0);playlistLogger.finishLoad(value,{sucesso:true,totalFaixas:data.total_faixas,nomePlaylist:data.nome_playlist,modo:data.modo});}catch(error:any){const message=error?.message||"Não foi possível carregar a playlist.";setPlaylistData(null);setTracks([]);setCurrentTrackIndex(null);setPlaylistError(message);playlistLogger.finishLoad(value,{sucesso:false,error:message});}finally{setIsLoadingPlaylist(false);}},[]);
  useEffect(()=>{loadPlaylist("top_hits");},[loadPlaylist]);
  const nextTrack=useCallback(()=>{const list=tracksRef.current,current=indexRef.current;if(!list.length||current===null)return;if(repeatRef.current==="one"){playTrack(current);return;}if(shuffleRef.current){let next=Math.floor(Math.random()*list.length);if(list.length>1&&next===current)next=(current+1)%list.length;playTrack(next);return;}if(current+1<list.length)playTrack(current+1);else if(repeatRef.current==="all")playTrack(0);else setCurrentTrackIndex(null);},[]);
  const playTrack=useCallback(async(index:number)=>{const list=tracksRef.current;if(index<0||index>=list.length)return;indexRef.current=index;setCurrentTrackIndex(index);const track=list[index];if(track.videoId){playerRef.current?.loadVideo(track.videoId);playerRef.current?.play();return;}setTracks(prev=>prev.map((t,i)=>i===index?{...t,isLoadingVideo:true,hasError:false}:t));try{const videoId=await resolveYouTubeVideoIdClient(track.nome_musica,track.nome_artista);if(!videoId)throw new Error("Vídeo não encontrado no YouTube.");setTracks(prev=>prev.map((t,i)=>i===index?{...t,videoId,isLoadingVideo:false}:t));playerRef.current?.loadVideo(videoId);playerRef.current?.play();}catch{setTracks(prev=>prev.map((t,i)=>i===index?{...t,isLoadingVideo:false,hasError:true}:t));if(list.length>1)setTimeout(()=>nextTrack(),300);}},[]);
  const previousTrack=useCallback(()=>{const list=tracksRef.current,current=indexRef.current;if(!list.length||current===null)return;if(currentTime>3){playerRef.current?.seekTo(0);return;}playTrack(current>0?current-1:list.length-1);},[currentTime]);
  const togglePlay=useCallback(()=>{if(currentTrackIndex===null){if(tracks.length)playTrack(0);return;}if(playbackStatus==="playing")playerRef.current?.pause();else playerRef.current?.play();},[currentTrackIndex,tracks.length,playbackStatus,playTrack]);
  const removeTrack=(index:number)=>{setTracks(prev=>prev.filter((_,i)=>i!==index));if(currentTrackIndex===index){const nextLength=tracks.length-1;if(nextLength<=0)setCurrentTrackIndex(null);else playTrack(Math.min(index,nextLength-1));}else if(currentTrackIndex!==null&&currentTrackIndex>index)setCurrentTrackIndex(currentTrackIndex-1);};
  const removeMultipleTracks=(indexes:number[])=>{const removed=new Set(indexes),old=tracksRef.current,current=indexRef.current,updated=old.filter((_,i)=>!removed.has(i));setTracks(updated);if(!updated.length){setCurrentTrackIndex(null);return;}if(current!==null&&removed.has(current))playTrack(0);else if(current!==null)setCurrentTrackIndex(updated.findIndex(t=>t===old[current]));};
  const currentTrack=currentTrackIndex===null?null:tracks[currentTrackIndex]||null;
  return <div className="min-h-screen bg-zinc-950 text-zinc-100 flex flex-col pb-24"><Navbar configStatus={configStatus} spotifyUser={spotifyUser} isLoggingIn={false} onLoginSpotify={()=>setSpotifyAuthOpen(true)} onLogoutSpotify={logoutSpotify} googleUser={null} isGoogleLoggingIn={false} onLoginGoogle={()=>{}} onLogoutGoogle={()=>{}} onOpenConfigModal={()=>setConfigOpen(true)}/><main className="flex-1 max-w-7xl w-full mx-auto px-3 sm:px-6 lg:px-8 py-3 sm:py-6 space-y-4"><PlaylistInput onLoadPlaylist={loadPlaylist} isLoading={isLoadingPlaylist} currentPlaylistId={playlistData?.playlist_id} spotifyUser={spotifyUser} userPlaylists={userPlaylists} isLoadingUserPlaylists={isLoadingUserPlaylists} onLoginSpotify={()=>setSpotifyAuthOpen(true)} onRefreshUserPlaylists={fetchUserPlaylists}/>{needsAuthNotice&&<div className="p-4 rounded-2xl bg-amber-950/50 border border-amber-500/40 flex items-center justify-between gap-4"><div className="flex items-center gap-3"><Lock className="w-5 h-5 text-amber-400"/><div><b className="text-white">Playlist restrita</b><p className="text-xs text-amber-200">Conecte a conta Spotify para acessar playlists privadas ou colaborativas.</p></div></div><button onClick={()=>setSpotifyAuthOpen(true)} className="px-4 py-2 rounded-xl bg-[#1DB954] text-zinc-950 font-bold text-xs flex items-center gap-2"><LogIn className="w-4 h-4"/>Conectar Spotify</button></div>}{playlistError&&!needsAuthNotice&&<div className="p-4 rounded-2xl bg-red-950/60 border border-red-500/50 text-red-200 text-sm flex gap-3"><AlertCircle className="w-5 h-5 shrink-0"/><span>{playlistError}</span></div>}{isLoadingPlaylist?<div className="p-16 rounded-2xl bg-zinc-900/50 border border-zinc-800 text-center"><Disc3 className="w-10 h-10 animate-spin mx-auto mb-3 text-emerald-400"/><p>Carregando playlist...</p></div>:<TrackList tracks={tracks} currentTrackIndex={currentTrackIndex} isPlaying={playbackStatus==="playing"} onPlayTrack={playTrack} onTogglePlayPause={togglePlay} onRemoveTrack={removeTrack} onRemoveMultipleTracks={removeMultipleTracks} shuffle={shuffle} onToggleShuffle={()=>setShuffle(v=>!v)} onPlayShuffle={()=>{setShuffle(true);if(tracks.length)playTrack(Math.floor(Math.random()*tracks.length));}} playlistName={playlistData?.nome_playlist} playlistCover={playlistData?.capa_playlist} playlistDescription={playlistData?.descricao} playlistNotice={playlistData?.aviso} isPrivate={playlistData?.isPrivate} autenticado={playlistData?.autenticado}/>}</main><AudioPlayerBar currentTrack={currentTrack} playbackStatus={playbackStatus} currentTime={currentTime} duration={duration} volume={volume} isMuted={isMuted} shuffle={shuffle} repeatMode={repeatMode} onTogglePlayPause={togglePlay} onPrevTrack={previousTrack} onNextTrack={nextTrack} onSeek={seconds=>{playerRef.current?.seekTo(seconds);setCurrentTime(seconds);}} onVolumeChange={value=>{setVolume(value);setIsMuted(value===0);playerRef.current?.setVolume(value);}} onToggleMute={()=>{if(isMuted){const value=previousVolume||50;setIsMuted(false);setVolume(value);playerRef.current?.setVolume(value);}else{setPreviousVolume(volume);setIsMuted(true);setVolume(0);playerRef.current?.setVolume(0);}}} onToggleShuffle={()=>setShuffle(v=>!v)} onToggleRepeat={()=>setRepeatMode(v=>v==="off"?"all":v==="all"?"one":"off")} onOpenNowPlaying={()=>{}}/><YouTubeIFrameContainer ref={playerRef} currentVideoId={currentTrack?.videoId} volume={isMuted?0:volume} onStatusChange={setPlaybackStatus} onTimeUpdate={(current,total)=>{setCurrentTime(current);if(total>0)setDuration(total);}} onTrackEnded={nextTrack} onError={()=>nextTrack()}/><SpotifyAuthModal isOpen={spotifyAuthOpen} onClose={()=>setSpotifyAuthOpen(false)} onLoginSuccess={handleSpotifyLoginSuccess} configStatus={configStatus}/><ConfigGuideModal isOpen={configOpen} onClose={()=>setConfigOpen(false)} configStatus={configStatus}/></div>;
}
