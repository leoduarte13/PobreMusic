import React,{useState}from"react";
import{X,Cloud,Plus,Check,Music2}from"lucide-react";
import{SavedPlaylist,Track}from"../types";
interface Props{isOpen:boolean;onClose:()=>void;tracks:Track[];playlists:SavedPlaylist[];onAdd:(playlist:SavedPlaylist,tracks:Track[])=>Promise<void>;onCreate:(name:string,tracks:Track[])=>Promise<void>}
export const AddTracksToPlaylistModal:React.FC<Props>=({isOpen,onClose,tracks,playlists,onAdd,onCreate})=>{const[name,setName]=useState("");const[busy,setBusy]=useState(false);if(!isOpen)return null;const add=async(p:SavedPlaylist)=>{setBusy(true);try{await onAdd(p,tracks);onClose()}finally{setBusy(false)}};const create=async()=>{if(!name.trim())return;setBusy(true);try{await onCreate(name.trim(),tracks);setName("");onClose()}finally{setBusy(false)}};return <div className="fixed inset-0 z-[60] flex items-center justify-center p-4 bg-black/80 backdrop-blur-md"><div className="w-full max-w-md bg-zinc-900 border border-zinc-800 rounded-2xl shadow-2xl p-5 text-zinc-100"><div className="flex items-center justify-between pb-4 border-b border-zinc-800"><div><h2 className="text-lg font-bold">Adicionar à playlist</h2><p className="text-xs text-zinc-400 mt-1">{tracks.length} música{tracks.length===1?"":"s"} selecionada{tracks.length===1?"":"s"}</p></div><button onClick={onClose} className="p-2 rounded-lg hover:bg-zinc-800"><X className="w-5 h-5"/></button></div><div className="py-4 space-y-2 max-h-64 overflow-y-auto">{playlists.length?playlists.map(p=><button key={p.id} disabled={busy} onClick={()=>add(p)} className="w-full p-3 rounded-xl bg-zinc-950 border border-zinc-800 hover:border-emerald-500/50 flex items-center gap-3 text-left"><div className="w-10 h-10 rounded-lg bg-emerald-500/10 flex items-center justify-center"><Cloud className="w-5 h-5 text-emerald-400"/></div><div className="min-w-0 flex-1"><b className="text-sm truncate block">{p.name}</b><span className="text-[11px] text-zinc-500">{p.tracks.length} faixas • Nuvem</span></div><Plus className="w-4 h-4 text-emerald-400"/></button>):<div className="p-5 text-center text-xs text-zinc-500"><Music2 className="w-7 h-7 mx-auto mb-2 text-zinc-600"/>Nenhuma playlist na nuvem ainda.</div>}</div><div className="pt-4 border-t border-zinc-800"><p className="text-xs font-bold text-zinc-300 mb-2">Criar nova playlist com as selecionadas</p><div className="flex gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome da nova playlist" className="flex-1 min-w-0 px-3 py-2.5 rounded-xl bg-zinc-950 border border-zinc-700 text-sm outline-none focus:border-emerald-500"/><button disabled={busy||!name.trim()} onClick={create} className="px-4 rounded-xl bg-emerald-600 hover:bg-emerald-500 disabled:opacity-40 font-bold text-xs flex items-center gap-1"><Check className="w-4 h-4"/>Criar</button></div></div></div></div>};
