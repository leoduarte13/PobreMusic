import React,{useEffect,useState}from"react";import{X,Cloud,Plus,Check,Music2,Loader2}from"lucide-react";import{SavedPlaylist,Track}from"../types";import{auth,fetchUserCloudPlaylists,saveUserPlaylistToCloud}from"../lib/firebase";
interface Props{isOpen:boolean;onClose:()=>void;tracks:Track[]}
export const AddTracksToPlaylistModal:React.FC<Props>=({isOpen,onClose,tracks})=>{const[playlists,setPlaylists]=useState<SavedPlaylist[]>([]);const[busy,setBusy]=useState(false);const[name,setName]=useState("");const[error,setError]=useState("");useEffect(()=>{if(!isOpen||!auth.currentUser)return;setError("");fetchUserCloudPlaylists(auth.currentUser.uid).then(setPlaylists).catch(e=>setError(e?.message||"Não foi possível carregar suas playlists."))},[isOpen]);if(!isOpen)return null;const add=async(p:SavedPlaylist)=>{if(!auth.currentUser)return;setBusy(true);setError("");try{const ids=new Set(p.tracks.map(t=>t.spotify_id||`${t.nome_musica}|${t.nome_artista}`));const unique=tracks.filter(t=>!ids.has(t.spotify_id||`${t.nome_musica}|${t.nome_artista}`));await saveUserPlaylistToCloud(auth.currentUser.uid,{...p,tracks:[...p.tracks,...unique],updatedAt:Date.now()});onClose()}catch(e:any){setError(e?.message||"Não foi possível salvar.")}finally{setBusy(false)}};const create=async()=>{if(!auth.currentUser||!name.trim()||!tracks.length)return;setBusy(true);setError("");try{const now=Date.now();await saveUserPlaylistToCloud(auth.currentUser.uid,{id:`cloud-${now}-${Math.random().toString(36).slice(2,8)}`,userId:auth.currentUser.uid,name:name.trim(),description:"",cover:tracks[0]?.capa||"",tracks:[...tracks],createdAt:now,updatedAt:now,isCloud:true});setName("");onClose()}catch(e:any){setError(e?.message||"Não foi possível criar a playlist.")}finally{setBusy(false)}};return <div className="fixed inset-0 z-[70] flex items-center justify-center p-4 bg-black/80 backdrop-blur-md"><div className="w-full max-w-md bg-zinc-900 border border-zinc-800 rounded-2xl p-5 text-zinc-100 shadow-2xl"><div className="flex justify-between items-center pb-4 border-b border-zinc-800"><div><h2 className="text-lg font-bold">Adicionar à playlist</h2><p className="text-xs text-zinc-400 mt-1">{tracks.length} selecionadas</p></div><button onClick={onClose} className="p-2 hover:bg-zinc-800 rounded-lg"><X className="w-5 h-5"/></button></div><div className="py-4 space-y-2 max-h-60 overflow-y-auto">{playlists.map(p=><button key={p.id} disabled={busy} onClick={()=>add(p)} className="w-full p-3 rounded-xl bg-zinc-950 border border-zinc-800 hover:border-emerald-500/50 flex items-center gap-3 text-left"><Cloud className="w-5 h-5 text-emerald-400"/><span className="flex-1 min-w-0"><b className="block truncate text-sm">{p.name}</b><small className="text-zinc-500">{p.tracks.length} faixas</small></span><Plus className="w-4 h-4"/></button>)}{!playlists.length&&!error&&<div className="py-5 text-center text-xs text-zinc-500"><Music2 className="w-7 h-7 mx-auto mb-2"/>Nenhuma playlist na nuvem.</div>}</div><div className="border-t border-zinc-800 pt-4"><p className="text-xs font-bold mb-2">Criar nova playlist</p><div className="flex gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome da playlist" className="flex-1 min-w-0 px-3 py-2.5 rounded-xl bg-zinc-950 border border-zinc-700 text-sm"/><button disabled={busy||!name.trim()} onClick={create} className="px-4 rounded-xl bg-emerald-600 disabled:opacity-40 font-bold text-xs">{busy?<Loader2 className="w-4 h-4 animate-spin"/>:<Check className="w-4 h-4"/>}</button></div>{error&&<p className="text-xs text-red-300 mt-2">{error}</p>}</div></div></div>};
