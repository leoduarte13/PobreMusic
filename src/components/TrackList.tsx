import React,{useState}from"react";import{Music,Loader2,Search,Disc3,Trash2,BookmarkPlus,Plus,Sliders,Download,Shuffle,Cloud,CheckSquare,CheckCircle2}from"lucide-react";import{Track}from"../types";import{Visualizer}from"./Visualizer";import{AddTracksToPlaylistModal}from"./AddTracksToPlaylistModal";
interface TrackListProps{tracks:Track[];currentTrackIndex:number|null;isPlaying:boolean;onPlayTrack:(index:number)=>void;onTogglePlayPause:()=>void;onRemoveTrack:(index:number)=>void;onRemoveMultipleTracks?:(indexes:number[])=>void;onOpenSaveModal?:()=>void;onOpenCreateModal?:()=>void;onOpenAddModal?:()=>void;onOpenAddTrackModal?:()=>void;onOpenEqualizerModal?:()=>void;onOpenMobileDownload?:()=>void;onToggleShuffle?:()=>void;onPlayShuffle?:()=>void;onOpenNowPlaying?:()=>void;shuffle?:boolean;playlistName?:string;playlistCover?:string;playlistDescription?:string;playlistNotice?:string;isPrivate?:boolean;autenticado?:boolean;isCloudSynced?:boolean}
export const TrackList:React.FC<TrackListProps>=({tracks,currentTrackIndex,isPlaying,onPlayTrack,onTogglePlayPause,onRemoveTrack,onRemoveMultipleTracks,onOpenSaveModal,onOpenAddModal,onOpenAddTrackModal,onOpenEqualizerModal,onPlayShuffle,onOpenNowPlaying,shuffle,playlistName,playlistCover,playlistNotice,isCloudSynced=true})=>{const[filterText,setFilterText]=useState("");const[removedNotification,setRemovedNotification]=useState<string|null>(null);const[downloadingTrackId,setDownloadingTrackId]=useState<number|null>(null);const[downloadedTrackIds,setDownloadedTrackIds]=useState<Set<number>>(new Set());const[selectedTrackIndexes,setSelectedTrackIndexes]=useState<Set<number>>(new Set());const[isSelectMode,setIsSelectMode]=useState(false);const[addModalOpen,setAddModalOpen]=useState(false);const handleOpenAdd=onOpenAddModal||onOpenAddTrackModal;const formatDuration=(ms?:number)=>{if(!ms)return"--:--";const s=Math.floor(ms/1000);return`${Math.floor(s/60)}:${String(s%60).padStart(2,"0")}`};const handleRemove=(e:React.MouseEvent,i:number,n:string)=>{e.stopPropagation();onRemoveTrack(i);setRemovedNotification(`"${n}" removida da playlist`);setTimeout(()=>setRemovedNotification(null),2000)};const toggleSelect=(e:React.MouseEvent,i:number)=>{e.stopPropagation();setSelectedTrackIndexes(p=>{const n=new Set(p);n.has(i)?n.delete(i):n.add(i);return n})};const selectAll=()=>setSelectedTrackIndexes(selectedTrackIndexes.size===tracks.length?new Set():new Set(tracks.map((_,i)=>i)));const bulkRemove=()=>{const a=Array.from(selectedTrackIndexes).sort((a,b)=>b-a);if(!a.length)return;if(onRemoveMultipleTracks)onRemoveMultipleTracks(a);else a.forEach(i=>onRemoveTrack(i));setSelectedTrackIndexes(new Set());setIsSelectMode(false);setRemovedNotification(`${a.length} faixas removidas da playlist`);setTimeout(()=>setRemovedNotification(null),2500)};const selectedTracks=Array.from(selectedTrackIndexes).sort((a,b)=>a-b).map(i=>tracks[i]).filter(Boolean);const download=async(e:React.MouseEvent,i:number,t:Track)=>{e.stopPropagation();setDownloadingTrackId(i);try{const c=`#EXTM3U\n#EXTINF:${t.duracao_ms?Math.floor(t.duracao_ms/1000):-1},${t.nome_artista} - ${t.nome_musica}\n${t.videoId?`https://www.youtube.com/watch?v=${t.videoId}`:`https://open.spotify.com/track/${t.spotify_id||""}`}\n`;const u=URL.createObjectURL(new Blob([c],{type:"audio/x-mpegurl"}));const a=document.createElement("a");a.href=u;a.download=`${t.nome_artista}_${t.nome_musica}`.replace(/[^a-z0-9]/gi,"_")+".m3u";a.click();URL.revokeObjectURL(u);setDownloadedTrackIds(p=>new Set(p).add(i));setTimeout(()=>setDownloadedTrackIds(p=>{const n=new Set(p);n.delete(i);return n}),2500)}finally{setDownloadingTrackId(null)}};const filtered=tracks.map((track,originalIndex)=>({track,originalIndex})).filter(({track})=>{const q=filterText.toLowerCase();return track.nome_musica.toLowerCase().includes(q)||track.nome_artista.toLowerCase().includes(q)||(track.album||"").toLowerCase().includes(q)});return <><div className="w-full bg-zinc-900/60 border border-zinc-800/80 rounded-2xl overflow-hidden shadow-xl"><div className="p-3.5 sm:p-5 border-b border-zinc-800/70"><div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3"><div className="flex items-center gap-3 min-w-0 flex-1">{playlistCover?<img src={playlistCover} alt={playlistName||"Capa"} className="w-14 h-14 sm:w-20 sm:h-20 rounded-xl object-cover"/>:<div className="w-14 h-14 sm:w-20 sm:h-20 rounded-xl bg-zinc-800 flex items-center justify-center"><Disc3 className="w-7 h-7 text-emerald-400"/></div>}<div className="min-w-0"><div className="flex items-center gap-1.5"><span className="text-[10px] font-bold uppercase text-emerald-400">SpotTube</span>{isCloudSynced&&<span className="text-[10px] px-2 py-0.5 rounded-full bg-emerald-500/10 text-emerald-300"><Cloud className="inline w-3 h-3"/> Nuvem</span>}</div><h1 className="text-sm sm:text-lg font-bold truncate">{playlistName||"Playlist Selecionada"}</h1><div className="text-[11px] text-zinc-400">{tracks.length} faixas • YouTube Audio Stream</div></div></div><div className="flex items-center gap-1.5 flex-wrap"><button type="button" onClick={()=>{setIsSelectMode(v=>!v);if(isSelectMode)setSelectedTrackIndexes(new Set())}} className={`h-9 px-3 rounded-lg text-xs font-semibold border ${isSelectMode?"bg-indigo-600 text-white":"bg-zinc-800 text-zinc-300"}`}><CheckSquare className="inline w-3.5 h-3.5 mr-1"/>{isSelectMode?"Cancelar":"Selecionar"}</button>{onOpenSaveModal&&<button type="button" onClick={onOpenSaveModal} className="h-9 px-3 rounded-lg bg-zinc-800 text-xs font-semibold border border-zinc-700"><BookmarkPlus className="inline w-3.5 h-3.5 mr-1 text-emerald-400"/>Salvar Nuvem</button>}{handleOpenAdd&&<button type="button" onClick={handleOpenAdd} className="h-9 px-3 rounded-lg bg-emerald-500 text-zinc-950 font-bold text-xs"><Plus className="inline w-4 h-4 mr-1"/>Adicionar Música</button>}{onPlayShuffle&&<button type="button" onClick={onPlayShuffle} className="h-9 px-3 rounded-lg bg-zinc-800 text-xs border border-zinc-700"><Shuffle className="inline w-3.5 h-3.5 mr-1"/>Aleatório</button>}{onOpenEqualizerModal&&<button type="button" onClick={onOpenEqualizerModal} className="h-9 px-3 rounded-lg bg-zinc-800 text-xs border border-zinc-700"><Sliders className="inline w-3.5 h-3.5 mr-1"/>EQ</button>}</div></div>{isSelectMode&&<div className="mt-3 p-2.5 rounded-xl bg-indigo-950/60 border border-indigo-700/60 flex items-center justify-between gap-2 flex-wrap"><div className="flex items-center gap-2"><button onClick={selectAll} className="px-2.5 py-1 rounded bg-indigo-900/80 text-xs font-semibold">{selectedTrackIndexes.size===tracks.length?"Desmarcar Todas":"Selecionar Todas"}</button><span className="text-xs font-bold text-indigo-200">{selectedTrackIndexes.size} de {tracks.length} selecionadas</span></div>{selectedTrackIndexes.size>0&&<div className="flex gap-1.5"><button onClick={()=>setAddModalOpen(true)} className="px-3 py-1 rounded-lg bg-emerald-600 text-white text-xs font-bold"><Plus className="inline w-3.5 h-3.5 mr-1"/>Adicionar à playlist</button><button onClick={bulkRemove} className="px-3 py-1 rounded-lg bg-red-600 text-white text-xs font-bold"><Trash2 className="inline w-3.5 h-3.5 mr-1"/>Remover</button></div>}</div>}{playlistNotice&&<div className="mt-2 p-2 rounded-lg bg-emerald-950/30 text-[11px] text-emerald-300">{playlistNotice}</div>}{removedNotification&&<div className="mt-2 p-2 rounded-lg bg-zinc-800 text-xs">{removedNotification}</div>}</div><div className="px-3 sm:px-4 py-2 border-b border-zinc-800/60"><div className="relative max-w-sm"><Search className="w-3.5 h-3.5 absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500"/><input value={filterText} onChange={e=>setFilterText(e.target.value)} placeholder="Filtrar músicas nesta playlist..." className="w-full h-9 pl-8 pr-3 bg-zinc-900 border border-zinc-800 rounded-lg text-xs"/></div></div><div className="divide-y divide-zinc-800/30">{filtered.map(({track,originalIndex})=>{const current=currentTrackIndex===originalIndex,playing=current&&isPlaying,selected=selectedTrackIndexes.has(originalIndex);return <div key={`${track.nome_musica}-${originalIndex}`} onClick={()=>isSelectMode?setSelectedTrackIndexes(p=>{const n=new Set(p);n.has(originalIndex)?n.delete(originalIndex):n.add(originalIndex)}):current?onTogglePlayPause():onPlayTrack(originalIndex)} className={`min-h-[52px] px-3 sm:px-5 py-2 flex items-center gap-3 cursor-pointer ${selected?"bg-indigo-950/40":""} ${current?"bg-emerald-950/30":""}`}><div className="w-7 flex justify-center">{isSelectMode?<button onClick={e=>toggleSelect(e,originalIndex)} className="w-5 h-5 rounded border flex items-center justify-center">{selected&&<CheckSquare className="w-3.5 h-3.5"/>}</button>:playing?<Visualizer isPlaying/>:<span className="text-xs text-zinc-500">{originalIndex+1}</span>}</div>{track.capa?<img src={track.capa} className="w-10 h-10 rounded-lg object-cover"/>:<div className="w-10 h-10 rounded-lg bg-zinc-800 flex items-center justify-center"><Music className="w-4 h-4"/></div>}<div className="min-w-0 flex-1"><p className="text-xs sm:text-sm font-semibold truncate">{track.nome_musica}</p><p className="text-[10px] sm:text-xs text-zinc-400 truncate">{track.nome_artista}</p></div><span className="text-[11px] font-mono text-zinc-400">{formatDuration(track.duracao_ms)}</span><button onClick={e=>download(e,originalIndex,track)} className="p-1.5 text-zinc-400">{downloadingTrackId===originalIndex?<Loader2 className="w-3.5 h-3.5 animate-spin"/>:downloadedTrackIds.has(originalIndex)?<CheckCircle2 className="w-3.5 h-3.5 text-emerald-400"/>:<Download className="w-3.5 h-3.5"/>}</button><button onClick={e=>handleRemove(e,originalIndex,track.nome_musica)} className="p-1.5 text-zinc-500 hover:text-red-400"><Trash2 className="w-3.5 h-3.5"/></button></div>})}</div></div><AddTracksToPlaylistModal isOpen={addModalOpen} onClose={()=>setAddModalOpen(false)} tracks={selectedTracks}/></>};
